{
  "name": "radar-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Radar aerei in tempo reale per mobile: mappa Leaflet, dati ADS-B (fonte configurabile, oggi adsb.fi)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "vite": "^6.3.5",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^3.1.4"
  }
}
